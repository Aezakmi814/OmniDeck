apiVersion: 1

providers:
  - name: OmniDeck
    orgId: 1
    folder: OmniDeck
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
