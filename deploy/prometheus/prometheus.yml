global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: sysfnos
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_token
    static_configs:
      - targets: [app:3000]
